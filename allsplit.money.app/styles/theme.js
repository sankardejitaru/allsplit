import { lightColors, darkColors, getColorsForMode, THEME_MODES } from "./palettes";

/** @deprecated Use useAppTheme().colors in components */
export const theme = { colors: lightColors };

export const COLORS = lightColors;

export { lightColors, darkColors, getColorsForMode, THEME_MODES };
